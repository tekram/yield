<%= form_for(@exam_code) do |f| %>
  <% if @exam_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_code.errors.count, "error") %> prohibited this exam_code from being saved:</h2>

      <ul>
      <% @exam_code.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
